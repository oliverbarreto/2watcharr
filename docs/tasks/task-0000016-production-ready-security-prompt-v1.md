# Task: FEATURE: Proiduction Ready Security
i want to now use docker to put this into production behind an nginx proxy manager to provide https. what are the steps needed in configuraation. I will provide a custom domain and have the docker server ready.

Also is the app ready in terms of security ? 

