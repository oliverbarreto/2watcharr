# Task: Only allow grid view in mobile

As you can see in the image below the list view in the channel page in mobile looks bad because the row cells are not the best way to utilize the space in mobile screens. 

The goal is to only allow the grid view in mobile which looks much better.