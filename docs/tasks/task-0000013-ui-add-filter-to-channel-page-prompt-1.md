i tested manually adding a podcast and it worked like a charm. lets now make a minor update in the channels page. Lets add a filter. Use similar layout of the  filter in the watch list. The filter should allow filtering by name, tag, and by type (youtube/podcast)

