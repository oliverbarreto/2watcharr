# Task: update docs, tag and release

Let's update documentation with recent bugfixes and features. Then let's merge develop into main and then let's bump the version (0.+1.0) tag and create a release in github

