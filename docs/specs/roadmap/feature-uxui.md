
- [ ] UI/UX: some ui modifications we need to implement:
    - [ ] show all the tags assigned to videos of a channel in the channel card
    - [ ] add important dates to cards (eg: when added, when watched, when favorited, when removed, etc.)
